{
  "name": "@repo/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "npx sst dev",
    "sleep5s": "node -e \"setTimeout(() => process.exit(0), 15000)\"",
    "dev": "npm run sleep5s && npm run backend",
    "build": "npx sst build",
    "deploy": "npx sst deploy",
    "remove": "npx sst remove",
    "console": "npx sst console",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "sst": "^2.43.3",
    "aws-cdk-lib": "2.142.1",
    "constructs": "10.3.0",
    "typescript": "^5.5.3",
    "@tsconfig/node18": "^18.2.4"
  }
}